[server]
# Bắt buộc phải set domain trống để proxy được
root_url = %(protocol)s://%(domain)s:%(http_port)s/
serve_from_sub_path = true

[security]
# Cho phép nhúng iframe (đây là key chính)
allow_embedding = true
cookie_samesite = none

[auth.anonymous]
enabled = true
org_role = Viewer
